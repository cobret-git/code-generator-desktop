<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="QuickCode.Controls.CodeGeneratorContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="using:QuickCode.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:data="using:QuickCode.Components.Data"
             xmlns:vm="using:QuickCode.ViewModel"
             d:DataContext="{d:DesignInstance vm:CodeGeneratorViewModel, IsDesignTimeCreatable=False}"
             mc:Ignorable="d">
    <UserControl.Resources>
    </UserControl.Resources>
    <Border VerticalAlignment="Center" HorizontalAlignment="Center" CornerRadius="8" Padding="16"
            Background="{ThemeResource LayerFillColorDefaultBrush}" MinWidth="600">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <Border Grid.Row="0" Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                    CornerRadius="8" Padding="16">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" x:Uid="CodeGenQrCodePreview" Style="{StaticResource BodyStrongTextBlockStyle}"
                               HorizontalAlignment="Center"/>
                </Grid>
            </Border>

            <Grid Grid.Row="1" ColumnSpacing="14" Margin="0,16,0,0" HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <RadioButton Grid.Column="0" GroupName="ButtonTabSelectionGroup" Content="Content" IsChecked="True"/>
                <RadioButton Grid.Column="1" GroupName="ButtonTabSelectionGroup" Content="Design"/>
            </Grid>

            <ComboBox Grid.Row="2" Margin="0,16,0,0" HorizontalAlignment="Stretch"
                      ItemsSource="{Binding CodeGenerationTypeOptions}"
                      SelectedItem="{Binding CodeGenType, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                <ComboBox.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Converter={StaticResource EnumToLocalizedTextConverter}}"/>
                        </StackPanel>
                    </DataTemplate>
                </ComboBox.ItemTemplate>
            </ComboBox>

            <TextBox Grid.Row="3" Header="Message:" Margin="0,16,0,0" AcceptsReturn="True" TextWrapping="Wrap" MinHeight="80"/>

            <Button Grid.Row="4" Content="Download QR Code" Margin="0,16,0,0" HorizontalAlignment="Stretch"/>
        </Grid>
    </Border>
</UserControl>
